SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# Set USE_SQLSERVER=True to use SQL Server, False to use SQLite
USE_SQLSERVER=False

# SQL Server Database Settings (only used when USE_SQLSERVER=True)
DB_HOST=localhost
DB_PORT=1433
DB_NAME=KIDOO
DB_USER=your_sqlserver_username
DB_PASSWORD=your_sqlserver_password
